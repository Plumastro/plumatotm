# Requirements for PLUMATOTM Astrological Animal Compatibility Engine
# Use with Python 3.11 for pre-compiled wheels

# Core astrological library
pyswisseph==2.08.00-1
flatlib>=0.2.3

# Data processing
pandas>=2.3.0
numpy>=2.3.0

# Date and time handling
python-dateutil>=2.9.0
pytz>=2025.0

# Geocoding (optional - for place name to coordinates conversion)
geopy>=2.4.0

# Timezone detection (for local to UTC conversion)
timezonefinder>=6.5.0

# Visualization (for radar charts)
matplotlib>=3.10.0
Pillow>=10.0.0
cairosvg>=2.7.0

# OpenAI API for ChatGPT interpretation
openai>=1.0.0

# Functional Requirements Specification:
# - Generate ChatGPT interpretation of why the top1 animal matches the subject's personality
# - Interpretation should be based on birth_chart.json and the 6 planets marked "TRUE" in top3_true_false.json for the top1 animal
# - Output should be a short argumentation (approx 800 characters total) in 3 bullet points
# - Result should be saved in a JSON file in the outputs directory
